# GeaStack GitHub Profile

This repository owns the GitHub organization profile for `geastack`.

GitHub renders the organization landing page from:

```text
profile/README.md
```

Keep this profile aligned with the website, the published npm packages, and the
current repository map.
